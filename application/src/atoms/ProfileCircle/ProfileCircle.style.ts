import styled from 'styled-components'
import { View } from 'react-native'

export const Circle = styled(View)({
  height: 35,
  width: 35,
  marginTop: -3,
  background: `orange`,
  borderRadius: 25
})
