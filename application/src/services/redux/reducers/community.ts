import * as A from '../actions'

const defaultState = {
  name: '',
  city: ''
}

const community = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default community
